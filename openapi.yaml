# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: User API
    description: The greeting service definition.
    version: 0.0.1
paths:
    /user/deregister:
        post:
            tags:
                - User
            description: mobile login
            operationId: User_DeRegister
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeRegisterRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeRegisterReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/login:
        post:
            tags:
                - User
            description: mobile login
            operationId: User_LoginByMobile
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginByMobileRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginByMobileReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/logout:
        post:
            tags:
                - User
            description: mobile login
            operationId: User_Logout
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LogoutRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LogoutReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/smscode:
        post:
            tags:
                - User
            description: Sends SMS code
            operationId: User_SendSMSCode
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SMSCodeRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SMSCodeReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        DeRegisterReply:
            type: object
            properties: {}
        DeRegisterRequest:
            type: object
            properties:
                smsCode:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        LoginByMobileReply:
            type: object
            properties:
                token:
                    type: string
                user:
                    $ref: '#/components/schemas/User'
        LoginByMobileRequest:
            type: object
            properties:
                mobile:
                    type: string
                clientId:
                    type: string
                smsCode:
                    type: string
        LogoutReply:
            type: object
            properties: {}
        LogoutRequest:
            type: object
            properties: {}
        SMSCodeReply:
            type: object
            properties: {}
        SMSCodeRequest:
            type: object
            properties:
                mobile:
                    type: string
                clientId:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        User:
            type: object
            properties: {}
tags:
    - name: User
